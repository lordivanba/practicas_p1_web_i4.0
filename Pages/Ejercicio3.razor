@page "/ejercicio3"
<div class="container">
    <h1 class="display-6 mb-2">Ejercicio 3</h1>

    <h4> Determinar en qué estado está el agua en función de su temperatura. Si es negativa el 
estado será sólido, si es menor que 100 será líquido y si es mayor que 100 será gas. Pedir al 
usuario el valor de la temperatura.</h4>

    <div class="row g-3 mx-5 px-5 my-2">
        <div class="col-md">
            <div class="form-floating">
                <input type="number" class="form-control form-control-lg" id="temperatura" placeholder="temperatura"
                    @bind-value="@temperatura">
                <label for="temperatura">Temperatura</label>
            </div>
        </div>
        <div>
            <button class="btn btn-primary btn-lg float-start" @onclick="calcular">Calcular</button>
        </div>
    </div>
</div>
@if (temperatura != null)
{
    <div class="container my-4">
        <h3>El estado del agua es: @estado</h3>
    </div>
}



@code {
    private double? temperatura { get; set; }
    private string? estado {get; set;}
    private void calcular(){
        if(temperatura < 0)
            estado = "Sólido";
        else if(temperatura >= 0 && temperatura < 100)
            estado = "Líquido";
        else
            estado = "Gaseoso";
    }
}